# 2026-08-13

## AGENTS.md 全面优化（核对 + 修补，第一批）
- 触发：用户要求分析 AGENTS.md 优化点 → 逐项核对证据后实施。核对结论：12 目录（pages.ts）/ strictPort（vite.config.ts）/ webServer 复用 5173 与 Pixel 7 仅跑 layout（playwright.config.ts）/ .gitignore 克隆注释 / ADR 13 篇均属实。
- **事实修正**：converter 模块映射表缺 monster_detail / achievements / version 三模块（MODULES 注册表实际 18 个）已补录；spine-lab「43 用例」数字过期已删（独立 vitest 配置）；router/index.ts 注释「13 个目录配置」与 pages.ts 实际 12 个不符 → 用户签发后修正。
- **结构**：新增术语速查声明（CONTEXT.md / docs/adr/ / docs/audit/ 引用关系）；「验证流程」提升为顶级章节（MUST 只留硬约束）；新增「新增目录/模块扩展指南（端到端）」。
- **去重 5 处**：query.py 命令、目录卡片模板字符串（仅留 MUST）、styles 全局声明、部署行数据同步说明、「下方」→「验证流程」节引用。
- **缺口补录**：commit message 规范（Conventional Commits）、无 ESLint/Prettier 声明、回滚操作（Vercel Rollback）、data-sync 每日 04:00 UTC 自动同步说明。
- **去历史/去硬化**：删除全部「2026-08-11/12」日期、Playwright 1.62、IndexedDB 历史；保留结论性坑位（`test:e2e:update` 必须显式 `=all`）。

## AGENTS.md 文件拆分（@ 导入，第二、三批）
- **平台能力验证**：Qoder 官方文档确认 `AGENTS.md` 支持 `@path/to/file` 导入（相对路径基于 AGENTS.md 目录；**递归展开 = include 语义，不减上下文**）；真正按需加载只有 `.qoder/rules/`（model_decision / glob 触发，但 Qoder 专属、牺牲跨平台可移植性）。用户选择标准方案。
- **变更**：主文件 297 → 153 行（架构节 156 行 → 17 行必知要点 + `@` 引用）；新建 docs/agents/architecture.md（89 行）/ data-pipeline.md（50 行）/ testing.md（26 行），各带「被主文件引用」头部说明。
- **教训**：`@` 引用必须裸写（行内代码/代码块中的 @ 不生效）；引用路径必须真实存在；@ 导入在**下次会话**才生效，主文件须保留降级摘要。

## 任务交付流程 8 → 5 精简（单人场景去仪式）
- 触发：用户指出单人数据展示型 wiki、避免过度设计。评估结论：原 8 条为「多人团队协作契约」形态，单人场景约一半是协调器（签发台账 / 里程碑 check-in / 结构化返工回传 / 复盘触发），真正要防的风险只有两个——AI 误解需求、AI 烧时间。
- **变更**：定级 L1/L2/L3 删除（留二分：默认直接做 vs 例外确认）；「3-5 条清单 + 判据类型标注」→「例外确认时列 ≤3 条验收标准」；签发仪式删除（并入验证）；返工 3 轮 → 两轮未果停下；复盘触发放宽（返工 ≥2 轮）。
- **引用修正**：验证流程 T2 行「L2/L3 按需」→「较大改动按需」；降级纪律「第 8 条复盘信号」→「第 5 条沉淀信号」；architecture.md 扩展指南「L2 常规任务」→「例外确认」类。docs/memory/2026-08-11.md 保留历史原貌。

## 任务交付流程二轮微调（交付收尾 + 去重 + 时机）
- 触发：二轮审查发现——「交付记录」概念悬空（验证流程要求降级在交付记录写明，但交付流程无交付环节）；第 3 条与验证流程纪律重复（禁全量 e2e 双写）；第 5 条无触发时机（当天 3 次流程改动零沉淀）；「偏离用户明确意图」判定前置性差（歧义才是真实触发点）；「两轮未果」的「轮」未定义。
- **变更**：②「意图有歧义」+「简述方案与可断言结果」（弱化清单形式）；③ 去重 + 补「收尾汇报：改动 diff + 验证结果 + 降级/豁免说明」三要素；④「两次修复尝试」；⑤ 补「收尾时命中」时机。
- **实证（跨会话价值）**：8→5 精简后「≤3 条验收标准清单」形式在后续会话从未被实践——单人交互中对齐天然发生在对话里，书面清单形式冗余；「收尾汇报三要素」是解决「交付记录」悬空的最小方案。

## 注释规范确立（面向 AI 接手者）
- 触发：用户明确「人类手动干预可能性很低，注释的主要对象是以后可接手的其他 AI」→ 注释规范不再单独考虑人类读者，按纯 AI 交接文档设计。
- **变更**：AGENTS.md「项目约定」新增 2 条——① 注释第一读者是后续 AI：写成可执行指令形态（禁止/必须/唯一收口），只写可验证事实，无证据不写「为什么」（防 AI 编造决策过程），禁复述实现、禁过期断言；② 注释落位分层：局部约束就近注释，跨文件/跨会话进 AGENTS.md 或 docs/memory/，不可逆决策进 ADR，术语进 CONTEXT.md；坑位必须当场注释。
- **设计依据**：AI 字面执行注释内容——过时/虚构注释比缺失危害更大（会传播错误指令）；而数据映射规则/路径特例/架构委约等代码无法自明的事实，注释是唯一就近防错闸门。

## 待办/遗留
- 拆分后 @ 导入需在下次会话验证展开是否生效（当前会话注入旧版全文）；若未展开，主文件必知要点 + 明确路径提供降级。
- docs/adr/ 13 篇未在 AGENTS.md 展开索引（术语速查引用声明已覆盖，按需读取）。