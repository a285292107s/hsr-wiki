# 2026-08-29

## 图片源反转：nanoka 主源 + jsDelivr 旧档回退（应对 jsDelivr fork 停更）

- 触发：用户报告 jsDelivr 镜像仓库（自建 fork `a285292107s/StarRailTextures`）已停止更新。实测冻结边界约在角色 1510（4.4 末）：本地数据 4.5（1512/1513）头像在 jsDelivr 404、nanoka 200。nanoka 持续更新但小部分旧档资产缺失（用户口径，反向缺口）。Step 2 直拼模式下（`USE_OFFICIAL_PATHS=true` + `cdnImgFallbackAttr` 恒空串）冻结后新增内容 404 无任何回退；且角色详情页立绘背景是 CSS `background-image`，不触发 error 事件，img 委托回退对它无效——只有主源直出可救。
- **决策（用户确认方案 A「主源反转 + 全量回退」，放弃最小改动方案 B）**：
  - `lib/constants.ts`：`USE_OFFICIAL_PATHS` 切回 `false`（icons.ts 的 Step 1 双源分支原样接管，零逻辑改动）。
  - `services/cdn/resolve.ts`：`remoteCdnUri` 反转为 nanoka 主源 + jsDelivr 回退（jd 规则命中才有回退 URL）；`cdnFallbackFromPrimary` 新增 nanoka→jsDelivr 正向反查（按注册子路径定位分类）；`cdnImgFallbackAttr` 删除 `USE_OFFICIAL_PATHS` 短路、常开计算。
  - `services/cdn/base.ts`：注册表新增 `jdPrimary` 字段——唯一使用场景 trace（nanoka 源为 146 字节占位图，沿用 08-28 坑位结论），其余分类禁止设置。
  - `lib/icons.ts`：新增 `avatarDrawCardJdUrl`（jsDelivr PNG 直链，不经开关）——HomeView 预载链第二源改用它；若沿用 `avatarDrawCardUrl` 会与 webp 主源同 URL，回退链退化。
- **坑位（必知）**：
  - 禁止在 `USE_OFFICIAL_PATHS=true` 模式上线：该模式无任何回退，fork 不恢复同步则新内容必破图。
  - jsDelivr 新角色 = 永久 404；nanoka 缺口 = 固定小集合且可由 jsDelivr 回退兜住。两源皆缺（如冻结后新增的 GridFight 属性图标）只能降级占位。
  - `app/catalog/pages/endgame.ts` 页签图仍直拼 `OFFICIAL_ICON_BASE`（无 nanoka 等价路径）——冻结后新赛季页签图为已知残留暴露，未处理。
  - GitHub API 匿名配额在本机环境常 403；两源覆盖差异用 HEAD 探测边界比查 API 可靠。
- **验证**：`pnpm test` 263 例全绿（cdn.test.ts 36 例 + format.test.ts 多处断言翻写到新优先级）；`pnpm build`（vue-tsc + 三守卫）全绿；nanoka 抽样 5 分类 HEAD 200（1512/1513 新角色、Rank_3、monster 等）。降级说明：T2 预期的 e2e 未跑——改动仅为 URL 字符串构造，无模板/DOM/数据流结构变化；像素基线因 png→webp 源切换可能漂移，需本机 `pnpm test:e2e:update =all` 刷新后跑 visual。
- **遗留**：fork 恢复同步（外部动作，用户选择暂不处理；恢复后按 jsdelivr.ts 头注释流程验证命中率，再评估是否切回直拼模式）。
