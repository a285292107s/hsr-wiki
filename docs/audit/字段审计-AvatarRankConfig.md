# 字段审计：AvatarRankConfig（星魂）

> 审计日期：2026-08-03
> 审计人：AI 生成解读卡 → 人工裁决（裁决列待填）
> 方法：全量扫描 606 条记录（主表 606 + LD 24，字段全集无差异）收集字段全集 + 值分布统计 + TextMap 上下文 + StarRailRes 基线差集
> 流程依据：`docs/audit/字段价值审计流程.md`（AvatarSkillConfig 试点后第二张表）

## 一、审计发现（先读这个）

| # | 发现 | 说明 |
|---|------|------|
| F1 | **`Trigger` 的 6 个 Hash 全部不在 TextMap 中** | 6 个唯一 Hash 用 `query.py --resolve` 未命中，直接加载 TextMapCHS.json 全量验证（有符号 + uint32 无符号双查）**确认不存在** → 不是展示文本，是内部触发枚举 ID |
| F2 | **`Trigger` 与 `Rank` 完全一一对应（全角色恒定）** | Rank1-6 各对应一个固定 Hash（101 个角色 × 6 个 Rank 完全一致）：R1=2089636447 / R2=523552506 / R3=-1042531435 / R4=1686351920 / R5=120267979 / R6=-1445815962。**角色间零区分度**，信息量 = Rank 序号本身 |
| F3 | **`SkillAddLevelList` 语义验证成功：key=技能 ID，值=加级数** | 非空 202/606；520 个 key 中 478 命中 `AvatarSkillConfig.SkillID`（92%），未命中的命中忆灵技能表 `AvatarServantSkillConfig`（如 1141525）。加级数值分布：+2 级 340 处 / +1 级 192 处。样例：希儿 R3 = `{100103: 2, 100101: 1}`（终结技+2、普攻+1） |
| F4 | **`UnlockCost` 的 ItemID = 角色专属星魂素材（隐藏物品）** | 86 种消费模式；ItemID 11001+（1001→11001、1002→11002…按角色分段）。`ItemConfig` 无 11xxx 段（不进背包），但 TextMap 有「【丹恒】的星魂」「【希露瓦】的星魂」等素材名文本 |
| F5 | **`RankAbility` 全库无引用** | 非空 71/606，值形如 `"Avatar_March7th_Rank02"`——全 ExcelOutput 仅本表自指，无任何消费方（grep 全库确认） |
| F6 | **基线差集触发闸门 2** | StarRailRes `character_ranks.json` 有 **`materials`**（=源 `UnlockCost`）与 **`level_up_skills`**（=源 `SkillAddLevelList`），本地均未输出 → 基线有本地无，强制 ⚪ 待定起跳，AI 建议 🟡 可选 |
| F7 | **基线 desc 内嵌数值 vs 本地 `#1[i]`+param_list 分离** | 基线样例「为三月七恢复6点能量」（数值写死），本地保留 `#1[i]` 占位 + param_list 动态替换——本地方案更优（数值随版本/等级可变），无需动作 |

## 二、解读卡（11 字段，裁决列待填）

> 裁决：同意/改档（写目标档位），或对「人话语义」纠错。

### 🟢 必收（6 个，AI 建议，人工抽查）

| 字段 | 出现率 | 人话语义 | 证据 | 置信度 | 建议 |
|------|--------|----------|------|--------|------|
| RankID | 100% | 星魂唯一 ID（{角色}{序号}） | 已输出(id) | 高 | 必收 |
| Rank | 100% | 星魂序号（1-6，每档 101 条均匀） | 已输出（字典 key） | 高 | 必收 |
| Name | 100% | 星魂名（「记忆中的你」） | 已输出(name)，TextMap 字面量 key 解析 | 高 | 必收 |
| Desc | 100% | 星魂效果描述（含 #1[i] 占位 + 富文本标签） | 已输出(desc)，clean=False 保留标签 | 高 | 必收 |
| Param | 100% | 星魂数值参数（387/606 非空，如 [6]、[0.24,3,320]） | 已输出(param_list)；100101 抽查：desc「#1[i]」=6 与 param 一致 | 高 | 必收 |
| IconPath | 100% | 星魂图标（576 个唯一，SkillIcon_{char}_Rank{N}.png） | 已输出(icon)，map_icon_path 映射 | 高 | 必收 |

### 🟡 可选（1 个，已裁决）

| 字段 | 出现率 | 人话语义 | 证据 | 置信度 | 建议 |
|------|--------|----------|------|--------|------|
| SkillAddLevelList | 100% | 星魂为技能加级映射（{SkillID: 1\|2}，202/606 非空，仅 R3/R5） | F3 + F6：基线有 `level_up_skills`；与 RatedRankID 主表侧互逆、忆灵侧独有 | 高 | ⚪ 待定（用户裁决：待关联展示 UI 立项时一并设计，见存疑表 #7） |

### 🔴 排除（4 个，AI 无权判定，已确认）

| 字段 | 出现率 | 人话语义 | 排除证据（闸门 1 类别） | 置信度 | 建议 |
|------|--------|----------|------------------------|--------|------|
| Trigger | 100% | 星魂触发条件标识（6 个 Hash 与 Rank1-6 恒定绑定） | ① 与 Rank 完全共变（101 角色全一致，角色间零区分度）；F1：TextMap 无文本；F2：全库无引用方 | 高 | ✅ 排除（用户 2026-08-03 确认） |
| RankAbility | 100% | 内部能力 key（「Avatar_March7th_Rank02」，71/606 非空） | ④ 仅被内部逻辑引用（F5：全库无引用方） | 高 | ✅ 排除（用户 2026-08-03 确认） |
| ExtraEffectIDList | 100% | 机器可读效果 ID 列表（136/606 非空，如 [10000003]） | ④ 与 AvatarSkillConfig.ExtraEffectIDList 同类（试点已确认排除：无 UI 直显，描述文本已覆盖） | 高 | ✅ 排除（用户 2026-08-03 确认） |
| UnlockCost | 100% | 星魂解锁材料（{ItemID: 11001+角色专属星魂素材, ItemNum: 1}） | ② 官方隐藏标记（F4：素材为隐藏物品，ItemConfig 无 11xxx 段、不进背包，玩家无法主动获取）；玩家获得星魂的唯一途径是抽取重复角色，解锁材料对玩家决策无价值 | 高 | ✅ 排除（用户 2026-08-03 复查改判） |

### ⚪ 待定（1 个，需 UI 设计立项确认）

| 字段 | 出现率 | 人话语义 | 现状证据 | 置信度 | 建议 |
|------|--------|----------|----------|--------|------|
| SkillAddLevelList | 100% | 星魂为技能加级映射（仅 R3/R5，值 1/2 级） | 与 RatedRankID 主表侧 100% 互逆；忆灵技能侧 292 个独有；desc 已含文本描述 | 高 | **待定**（用户裁决：待关联展示 UI 立项） |

## 三、裁决记录（2026-08-03 完成）

**裁决结论**
- ✅ 6 个 🟢 必收：全部确认（已输出，抽查通过）
- ✅ 0 个 🟡 可选：SkillAddLevelList 改判待定（用户裁决），归入存疑表 #7（待关联展示 UI 立项）；UnlockCost 收录后改判排除（见下）
- ✅ 4 个 🔴 排除：全部确认（Trigger / RankAbility / ExtraEffectIDList / UnlockCost，未提异议）
- ⚪ 1 个待定：SkillAddLevelList 改判待定（用户裁决），归入存疑表 #7（待关联展示 UI 立项）

**已裁决记录**
- ✅ UnlockCost → 🟡 可选收录 → 🔴 **排除**（改判）——用户 2026-08-03 复查确认：星魂素材为官方隐藏物品（不进背包、无法主动获取），玩家获得星魂的唯一途径是抽取重复角色，解锁材料对玩家决策无价值（原收录依据 F6 基线 `materials` 为数据完整性导向，非玩家价值导向）
- ✅ SkillAddLevelList → ⚪ 待定——用户 2026-08-03 确认（与 RatedRankID 互逆、忆灵侧独有；待 UI 立项）
- ✅ Trigger / RankAbility / ExtraEffectIDList → 🔴 排除——用户 2026-08-03 确认
- ✅ 6 个 🟢 必收抽查通过

## 四、闸门 2 差集记录

| 基线字段（StarRailRes character_ranks.json） | 本地状态 | 处理 |
|----------------------------------------------|----------|------|
| id / name / rank / desc / icon | 本地有 | ✅ |
| materials | 本地无（源字段 UnlockCost） | 🔴 已排除（2026-08-03 复查改判：隐藏物品、玩家无决策价值，符合闸门 2 例外条款「有明确的有意排除理由」） |
| level_up_skills | 本地无（源字段 SkillAddLevelList） | ⚪ 待定 → AI 建议 🟡 可选，待裁决 |

> 反向检查：本地输出字段（id/name/desc/icon/param_list）全部有基线对应，无「本地有基线无」的越界输出。

## 五、闸门 3 误杀率统计（裁决完成）

- AI 建议 🔴 排除：3 个
- 人工改判：0 个
- **误杀率：0%**（≤ 5% 门槛 ✅ 通过）
- 备注：UnlockCost 为 AI 建议 🟡 收录 → 人工复查改判 🔴 排除（反向改判，不属误杀统计范围，但已归档理由）

## 六、结论与下一步

**本表审计要点（2026-08-03）**：
1. `Trigger` 为全库唯一发现的无文本、无引用、角色间零区分度的内部枚举——排除证据比试点 `CoolDown`（全常量）更强；
2. `UnlockCost` 先收录后改判排除（同日复查）：星魂素材为官方隐藏物品（ItemConfig 无 11xxx 段、不进背包），玩家无法主动获取，仅通过抽取重复角色间接获得——对玩家决策无价值，撤销转换器输出与前端展示；
3. `SkillAddLevelList` 改判待定：与 `RatedRankID`（已收录）主表技能侧 100% 互逆（228=228），忆灵技能侧 292 个为独有信息——待「星魂↔技能关联展示」UI 立项时一并设计。

**下一步（按审计边界优先级）**：
1. 常规模式下一张源表：AvatarSkillTreeConfig（行迹树）；
2. ⚪ 待定 SkillAddLevelList 归存疑表 #7；
3. 裁决记录已归档，误杀率 0% 维持流程放行。
