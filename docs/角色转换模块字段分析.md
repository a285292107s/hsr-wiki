# 角色转换模块字段分析

> **定位**：转换模块（converter）侧的角色字段活文档——逐字段说明"输出字段 → 源字段 → 转换逻辑 → 消费方 → 验证状态"。
> 与 [角色相关数据深挖](./角色相关数据深挖.md)（源数据侧全目录分布）互补：前者看"数据从哪来"，本文看"转换器怎么处理"。
>
> **本次深度审计**：2026-08-02（分支 `feature/character-converter-audit`）

---

## 一、范围与基准

### 范围

常规模式 4 个角色模块（货币战争角色模块下次单独审计）：

| 模块 | 输出文件 | 源数据 |
|------|----------|--------|
| `characters` | `characters.json` | AvatarConfig(+LD) |
| ~~`character_ranks`~~ | ~~`character_ranks.json`~~ | ~~AvatarRankConfig~~（**2026-08 已废弃删除**，见存疑 1） |
| ~~`character_skills`~~ | ~~`character_skills.json`~~ | ~~AvatarSkillConfig~~（**2026-08 已废弃删除**，见存疑 1） |
| `character_detail` | `characters/{id}.json`（95 个） | 19 个源文件联合 |

### 正确性基准

- **忠实性**：输出值与源数据一致（结构全量对比 95 角色 + 数值抽样 7 角色）
- **语义性**：字段含义正确（已知陷阱专项核查）
- 不引入外部数据基准（源数据即真相；特殊数值待人工核验时标注）

### 抽样角色（覆盖特殊机制分支）

| 角色 ID | 覆盖分支 |
|---------|----------|
| 1101 希儿 | 标准角色基线 |
| 8001 开拓者•毁灭 | 命名改写、StoryAtlas 非标准 StoryID |
| 1005 卡芙卡 | 行迹 `1{avatar_id}` 伪目录归一 |
| 1308 黄泉 | 常规机制 |
| 1412 阿格莱亚 | 忆灵（早期版本） |
| 1415 昔涟 | 忆灵（最新版本）+ 多技能 |
| 1510 姬子•启行 | 助战技（Assist 豁免 HideInUI 过滤） |

---

## 二、characters.json（角色索引）

| 字段 | 类型 | 源字段 | 转换逻辑 | 消费方 | 验证 |
|------|------|--------|----------|--------|------|
| `id` | number | `AvatarID` | 过滤：`Release=false` 跳过；非 1xxx-9xxx ID 跳过 | 角色目录/详情入口 | ✅ |
| `name` | string | `AvatarName`(Hash) | TextMap 解析；`开拓者` 时拼接 `·{命途}`（`开拓者·毁灭`） | 目录卡片 | ✅ 抽查 |
| `full_name` | string | `AvatarFullName`(Hash) | TextMap 解析；开拓者时同步改写 | 详情页 | ✅ |
| `rarity` | number | `Rarity` | `RARITY_MAP` 枚举映射（4/5 星 → 数字） | 目录筛选 | ✅ |
| `path` | string | `AvatarBaseType` | 原样（英文键，如 `Warlock`） | 目录筛选/图标 | ✅ |
| `element` | string | `DamageType` | 原样（英文键） | 目录筛选/图标 | ✅ |
| `sp_need` | number \| null | `SPNeed`(Value) | `unwrap_value`；**字段缺失时输出 null**（遐蝶 1407） | 详情页能量行 | ✅ 已修复 |
| `vo_tag` | string | `AvatarVOTag` | 原样 | 语音相关 | ✅ |
| `icon` | string | `DefaultAvatarHeadIconPath` | `map_icon_path` 前缀映射 | 目录卡片 | ✅ |
| `icon_round` | string | `AvatarSideIconPath` | `map_icon_path` | 目录卡片 | ✅ |
| `icon_mini` | string | `AvatarMiniIconPath` | `map_icon_path` | 小头像 | ✅ |
| `icon_cutin` | string | `AvatarCutinImgPath` | `map_icon_path` | 详情头图 | ✅ |
| `rank_ids` | number[] | `RankIDList` | 原样 | 详情星魂 | ✅ |
| `skill_ids` | number[] | `SkillList` | 原样 | 详情技能 | ✅ |

---

## 三、~~character_ranks.json~~（已废弃删除，2026-08）

> 该模块已删除：第二期 `character_detail` 在 `characters/{id}.json` 内自建了质量更高的星魂数据（见 5.3），前端全面改用详情数据，此索引无消费方。历史记录留存如下。

| 字段 | 类型 | 源字段 | 转换逻辑 | 消费方 | 验证 |
|------|------|--------|----------|--------|------|
| `id` | number | `RankID` | 原样 | ⚠️ 前端未消费（详情页用 `detail.ranks`） | ✅ |
| `rank` | number | `Rank` | 原样（1-6） | 同上 | ✅ |
| `name` | string | `Name`(字面量) | TextMap 解析（key 直接命中） | 同上 | ✅ |
| `desc` | string | `Desc`(字面量) | TextMap 解析 + **`clean_text` 清洗** | 同上 | ⚠️ 见存疑 1 |
| `icon` | string | `IconPath` | `map_icon_path` | 同上 | ✅ |

> 与 `detail.ranks` 的差异：详情版 `desc` 走 `clean=False`（保留 `<color>` 等标签由前端渲染），本索引版被清洗为纯文本。

---

## 四、~~character_skills.json~~（已废弃删除，2026-08）

> 该模块已删除：第二期 `character_detail` 在 `characters/{id}.json` 内自建了质量更高的技能数据（见 5.2），前端全面改用详情数据，此索引无消费方。历史记录留存如下。

| 字段 | 类型 | 源字段 | 转换逻辑 | 消费方 | 验证 |
|------|------|--------|----------|--------|------|
| `id` | number | `SkillID` | 同一 SkillID 多等级记录**只取第一条** | ⚠️ 前端未消费（详情页用 `detail.skills`） | ⚠️ 见存疑 2 |
| `name` | string | `SkillName`(Hash) | TextMap 解析 | 同上 | ✅ |
| `tag` | string | `SkillTag`(Hash) | TextMap 解析 | 同上 | ✅ |
| `type` | string | `AttackType` | `SKILL_TYPE_MAP` 映射，**未命中返回空串** | 同上 | ⚠️ 见存疑 2 |
| `type_name` | string | `SkillTypeDesc`(Hash) | TextMap 解析 | 同上 | ✅ |
| `desc` | string | `SkillDesc`(Hash) | TextMap 解析 + 清洗 | 同上 | ✅ |
| `simple_desc` | string | `SimpleSkillDesc`(Hash) | TextMap 解析 + 清洗 | 同上 | ✅ |
| `icon` | string | `SkillIcon` | `map_icon_path` | 同上 | ✅ |
| `max_level` | number | `MaxLevel` | 原样 | 同上 | ✅ |

---

## 五、characters/{id}.json（角色详情）

### 5.1 顶层字段

| 字段 | 类型 | 源字段 | 转换逻辑 | 消费方 | 验证 |
|------|------|--------|----------|--------|------|
| `name` | string | `AvatarName` | 同索引（含开拓者改写） | 详情页头图 | ✅ |
| `desc` | string | StoryAtlas 首条故事 | TextMap 解析 + 取第一句 | 详情页简介 | ✅ 已修复 |
| `chara_info` | object | 多源 | 见 5.8 | 详情页档案 | ✅ |
| `rarity` | string | `Rarity` | 原样（字符串键，未映射数字） | 详情页 | ✅ |
| `avatar_vo_tag` | string | `AvatarVOTag` | 原样 | — | ✅ |
| `sp_need` | number \| null | `SPNeed` | 同索引（缺失输出 null） | 详情页能量行 | ✅ 已修复 |
| `base_type` | string | `AvatarBaseType` | 原样 | 命途标识 | ✅ |
| `damage_type` | string | `DamageType` | 原样 | 属性标识 | ✅ |
| `ranks` | object | AvatarRankConfig | 见 5.3 | 星魂页 | ✅ |
| `skills` | object | AvatarSkillConfig(+LD) | 见 5.2 | 技能页 | ✅ |
| `skill_trees` | object | AvatarSkillTreeConfig(+LD) | 见 5.4 | 行迹页 | ✅ |
| `enhanced` | null | — | 占位（第一期不做） | — | ✅ |
| `memosprite` | object \| null | AvatarServantConfig(+Skill) | 见 5.6 | 忆灵页 | ✅ |
| `unique` | object | — | 占位 `{}` | — | ✅ |
| `stats` | object | AvatarPromotionConfig(+LD) | 见 5.5 | 成长页 | ✅ |
| `relics` | object | AvatarRelicRecommend(+LD) | 见 5.7 | 配装页 | ✅ |
| `lightcones` | number[] | AvatarEquipRecommend(+LD) | 推荐光锥 ID 列表 | 配装页 | ✅ |
| `teams` | [] | — | 占位空数组 | — | ✅ |
| `skin` | object | — | 占位 `{}` | — | ✅ |

### 5.2 skills（技能字典）

结构：`{ SkillID: Skill }`，Skill 字段：

| 字段 | 类型 | 源字段 | 转换逻辑 | 验证 |
|------|------|--------|----------|------|
| `id` | number | `SkillID` | — | ✅ |
| `name` | string | `SkillName`(Hash) | TextMap | ✅ |
| `desc` / `simple_desc` | string | `SkillDesc` / `SimpleSkillDesc` | TextMap，**`clean=False`**（保留标签前端渲染） | ✅ |
| `type` | string | `SkillTriggerKey` 优先 → `AttackType` 兜底 | 映射表；`Skill31-34` 内部子技能过滤；`HideInUI` 过滤（**Assist 豁免**）；未知槽位（如 SkillP02）无 AttackType 时透传 **null**（见风险表 #10） | ✅ |
| `type_name` | string | `SkillTypeDesc` | TextMap | ✅ |
| `tag` | string \| null | `SkillTag`(Hash) | **官方中文标签直出**（如「单攻」「召唤」），空转 null（方案 A，见 ADR 0008；曾用 SkillEffect 枚举 + 前端映射，已废弃） | ✅ |
| `sp_base` | number \| null | `SPBase`(Value) | `unwrap_value` | ✅ |
| `bp_need` / `bp_add` | number \| null | `BPNeed` / `BPAdd` | `unwrap_value` | ✅ |
| `show_stance_list` | number[] \| null | `ShowStanceList` | 逐项 `unwrap_value`，空转 null | ✅ |
| `skill_combo_value_delta` | number \| null | `SkillComboValueDelta` | 原样 | ✅ |
| `extra` | object | — | 预留 `{}` | ✅ |
| `level` | object | 同 SkillID 多等级记录 | `{ 等级: { level, param_list } }`，等级升序 | ✅ |

> 参数语义备忘：`ParamList` 逐项解包为纯数字，与描述文本中的 `#1[i]` 占位一一对应。

### 5.3 ranks（星魂）

结构：`{ Rank序号: { id, name, desc, icon, param_list, extra } }`

| 字段 | 源字段 | 要点 | 验证 |
|------|--------|------|------|
| `id` | `RankID` | — | ✅ |
| `name` | `Name`(字面量) | TextMap | ✅ |
| `desc` | `Desc`(字面量) | TextMap + `clean=False` | ✅ |
| `icon` | `IconPath` | `map_icon_path` | ✅ |
| `param_list` | `Param` | 逐项 `unwrap_value` | ✅ |
| `extra` | — | 预留 `{}` | ✅ |

### 5.4 skill_trees（行迹树）

结构：`{ Anchor: { 等级: Node } }`，Node 字段：

| 字段 | 类型 | 源字段 | 要点 | 验证 |
|------|------|--------|------|------|
| `anchor` | string | `AnchorType`/`PointTriggerKey` | 分组键 | ✅ |
| `avatar_promotion_limit` / `avatar_level_limit` | number \| null | 原样 | 解锁条件 | ✅ |
| `default_unlock` | bool | `DefaultUnlock` | — | ✅ |
| `icon` | string | `IconPath` | `map_icon_path` + **`1{avatar_id}` 伪目录归一** | ✅ |
| `level_up_skill_id` / `material_list` | array | 原样 | — | ✅ |
| `max_level` | number | `MaxLevel` | — | ✅ |
| `param_list` | array | `ParamList` | 逐项 `unwrap_value` | ✅ |
| `point_id` | number | `PointID` | — | ✅ |
| `point_name` / `point_desc` | string | `PointName` / `PointDesc` | TextMap；desc `clean=False` | ✅ |
| `point_trigger_key` / `point_type` | string \| null | 原样 | — | ✅ |
| `pre_point` | array | `PrePoint` | 前置点 | ✅ |
| `status_add_list` | array | `StatusAddList` | `[{ property_type, value, name }]`，value 解包、name TextMap | ✅ |
| `extra` | object | — | 预留 `{}` | ✅ |

> **进阶行覆盖**：同一 (AvatarID, Anchor, Level) 的进阶重复行（`PointID` 带 +1e7 偏移、`EnhancedID=1`）覆盖基础行——游戏内正确版本必须保留。已确认覆盖后输出 18 个 PointID（源含 36 条含进阶行）。

### 5.5 stats（突破属性）

结构：`{ "0"~"6": { attack_base, attack_add, defence_base, defence_add, hp_base, hp_add, speed_base, critical_chance, critical_damage, base_aggro, cost } }`

- 源：AvatarPromotionConfig(+LD)，按 `MaxLevel` 升序编号阶段 0-6
- 全部数值字段 `unwrap_value`；`cost` 为 PromotionCostList 原样
- 抽查 0/3/6 阶段 HPBase/AttackBase 与源一致 ✅

### 5.6 memosprite（忆灵）

| 字段 | 源字段 | 要点 | 验证 |
|------|--------|------|------|
| `name` | `ServantName`(Hash) | TextMap | ✅ |
| `icon` | `ActionServantHeadIconPath` | **原样未走 `map_icon_path`**（CDN 由前端拼接） | ✅ |
| `hp_base` / `speed_base` 等 | 原样（字符串） | 未解包（源即为字符串数字） | ✅ |
| `aggro` | `Aggro`(Value) | `unwrap_value` | ✅ |
| `skills` | AvatarServantSkillConfig | 与角色技能不同：**不过滤** HideInUI/TriggerKey；`type` 仅 `Servant` 或 null；`tag` 同为官方 SkillTag（如「群攻」） | ✅ |

> 归属规则：`ServantID - 10000 = 基础角色 ID`；开拓者 8xxx 同时分配给配对奇偶变体（18007 → 8007 & 8008）。

### 5.7 relics（遗器推荐）

`{ avatar_id, set4_id_list, set2_id_list, property_list3~6, property_list[{relic_type, property_type}], sub_affix_property_list, score_rank_list }` —— 全部透传 AvatarRelicRecommend(+LD)，按 AvatarID 命中。✅

### 5.8 chara_info（档案）

| 字段 | 源字段 | 要点 | 验证 |
|------|--------|------|------|
| `camp` | AvatarAtlas → `CampID` → AvatarCamp | 两级索引查名称 | ✅ |
| `va.{chinese/japanese/korean/english}` | AvatarAtlas `CV_CN/JP/KR/EN` | TextMap | ✅ |
| `stories.{0-4}` | StoryAtlas | **按 StoryID 升序取前 5 条**（修复：开拓者 StoryID=11-15 不再丢失） | ✅ 已修复 |
| `voicelines` | — | 占位 `[]` | ✅ |

### 5.9 占位/预留字段

`enhanced=null`、`unique={}`、`teams=[]`、`skin={}`、`chara_info.voicelines=[]`、`skill.extra={}`、`rank.extra={}`——CDN 兼容占位，前端按空值处理。✅

---

## 六、风险字段核查表（语义陷阱专项）

| # | 字段/机制 | 陷阱 | 处置 | 状态 |
|---|-----------|------|------|------|
| 1 | `SPNeed` | 部分角色（遐蝶 1407）**源无此字段**，此前输出 `{}` 破坏前端 `number` 类型契约 | 缺失输出 `null`，前端 `?? 0` 兜底 | ✅ 本次修复 |
| 2 | StoryAtlas `StoryID` | 开拓者（8xxx）StoryID=**11-15**，旧逻辑按 1-5 硬编码 → 简介+故事全丢 | 按 StoryID 升序取前 5 条 | ✅ 本次修复 |
| 3 | 行迹 `1{avatar_id}` 伪目录 | 进阶行 IconPath 指向 `1{avatar_id}` 目录（CDN 不存在 → 404） | `_normalize_tree_icon` 仅对 `1{avatar_id}` 归一 | ✅ 已确认 |
| 4 | 跨 ID 图标引用 | 开拓者偶数变体（8002 等）无图标资产，引用配对奇数 ID | **不得归一**（8002 的图标保留指向 8001） | ✅ 已确认 |
| 5 | Assist 助战技 | 姬子•启行（1510）助战技带 `HideInUI=true`，直接过滤会丢技能 | Assist 豁免过滤 | ✅ 已确认 |
| 6 | 进阶行重复记录 | 同一 (Avatar, Anchor, Level) 两条记录（PointID +1e7） | 覆盖保留进阶行版本 | ✅ 已确认 |
| 7 | `{F#}{M#}` 性别占位 | 简介/文本含 `{F#少女}{M#少年}` | 转换器**保留原样**，前端 `stripTags`/`fmtDesc` 提取 | ✅ 分工确认 |
| 8 | `clean` 参数分工 | 详情文本 `clean=False`（保留 `<color>`/`<unbreak>`/`<i>`） | 前端富文本渲染；索引文本清洗为纯文本 | ✅ 分工确认 |
| 9 | 行迹 Point 变体 | 13 个角色行迹点数量/命名不同（18~21 个，含 `Point22` 变体） | 新版本行迹树扩展，输出忠实 | ⚠️ 前端渲染需确认（见存疑 3） |
| 10 | SkillP02「天赋 2」无 AttackType | `get("AttackType", "")` 曾把缺失转空串 → 前端 `SKILL_ORDER.includes('')` 过滤隐藏（白厄 140805 等 3 角色） | 空串转 `null` 输出，前端独立成组显示；契约测试锁定 | ✅ 2026-08-02 修复 |
| 11 | 行迹属性节点图标 `SpriteOutput/UI/Avatar/Icon/` 未映射 | 950 处（全部角色属性强化节点） | **无实际影响**：point_desc 全空不进入 TALENTS 渲染，STAT BONUSES 走前端 PROP_ICON；仅转换日志噪音 | ✅ 关闭 |

---

## 七、发现-修复记录（2026-08-02 本次审计）

| # | 问题 | 影响 | 修复 | 验证 |
|---|------|------|------|------|
| 1 | `sp_need` 输出 `{}`（源字段缺失时 `item.get(..., {})` 默认值被解包） | 遐蝶详情/索引 `sp_need={}` 违反前端 `number` 类型；`?? 0` 兜底失效 | `item.get("SPNeed")` 去掉默认值 → 输出 `null`；types.ts 双处类型放宽为 `number \| null` | ✅ 1407 输出 null，1101 保持 120；build/测试全绿 |
| 2 | 开拓者故事全丢（StoryID 11-15 被 `1<=sid<=5` 过滤） | 8001-8010 简介为空、档案故事全 null | 改为按 StoryID 升序取前 5 条映射槽位 | ✅ 8001 desc/stories 全非空；1101 不受影响 |
| 3 | 技能标签链路（SkillTag 未用） | tag 输出英文枚举 + 前端 TAG 映射：缺 `Summon`（召唤系标签空白）、Impair→削弱/Restore→恢复 文案漂移 | 改用官方 SkillTag 中文直出（方案 A，ADR 0008）；前端删 TAG 映射，韧性条独立 STANCE_TAG | ✅ 1101=单攻/1415 终结技=召唤/忆灵=群攻；build+测试全绿 |
| 4 | 天赋 2（SkillP02）被隐藏 | 3 个角色（1408/1508/1509）无 AttackType → 空串 type 被前端过滤 | 空串转 null（风险表 #10），独立成组显示 | ✅ 140805 type=null |

历史修复（前序轮次，本报告范围内相关）：增量依赖声明漂移、currency 输出统一紧凑格式、原子写入、AST 依赖一致性测试等，见 tools/converter/README.md。

---

# 八、存疑项研究结论（2026-08-02 复核）

| # | 存疑 | 研究结论 | 建议动作 |
|---|------|----------|----------|
| 1 | `character_ranks.json` / `character_skills.json` 前端未消费 | **确认遗留并已删除（2026-08）**：前端 `src/` 零引用（grep 全库无匹配）；git 历史为第一期引入（`5d7e155`），ADR 0006 记录为"第一期只含 name/desc/icon"；第二期详情数据（`characters/{id}.json`）出现后前端全面改用详情——`character_detail` 自建 ranks/skills（全等级参数、HideInUI/内部技能过滤、保留富文本标签），两索引无消费方 | ✅ 已执行删除（转换器模块 + 输出文件 + ADR 标注） |
| 2 | `character_skills.json` 无 HideInUI/内部技能过滤 | **确认质量缺陷但无影响面，随 #1 一并消除**：688 个技能含 27 个 HideInUI + 13 个 Skill31-34 内部技能；112 个 `type` 空串（AttackType 未命中映射，如忆灵 Servant 技能；详情版兜底原值）——因前端未消费，缺陷不造成线上影响 | ✅ 随 #1 删除 |
| 3 | 行迹 Point 变体（13 角色）渲染 | **确认无问题**：变体为 5 个 20 点（1402/1407/1409/1413/1415）+ 6 个 19 点含 `Point22`（1501/1502/1505/1506/8009/8010）+ 2 个 21 点（8007/8008）；前端 `abilities` computed 动态遍历 `skill_trees` 全部 anchor、属性加成聚合亦动态，无点数硬编码 | 关闭（无需动作）；新角色行迹树扩展属正常演进 |
| 4 | `sp_need=null` 的展示语义 | **确认设计使然，存疑关闭**：遐蝶（1407）不依赖传统能量条，终结技通过全队生命损失积攒【新蕊】资源激活（TextMap 佐证：“我方全体目标每损失 1 点生命值遐蝶获得 1 点【新蕊】，当【新蕊】达到上限时可激活终结技”）——源数据无 `SPNeed` 是正确表达，输出 null、前端 `?? 0` 显示 0 均正确；若追求语义完整，前端可将遐蝶类角色能量行标注为【新蕊】机制（超出转换器范围） | 关闭；前端标签可选项：对 `sp_need=null` 的角色展示“新蕊”类资源文案 |
| 5 | **下一阶段字段补充清单**（本次审计确认的重要遗留字段，2026-08-02） | ① 星魂 `UnlockCost`（解锁材料，如 11308×1，游戏内星魂页可见）；② `MaxPromotion`/`MaxRank`（突破/星魂上限）；③ `AvatarCutinIntroText`（立绘引言）；④ `PlayerLevelRequire`（突破需开拓等级）；⑤ 技能 `LevelUpCostList`/`CoolDown`/`StanceDamageDisplay`/`UltraSkillIcon` 等次要字段；⑥ 行迹 `RecommendPriority` | 列入下一阶段计划，需前端 UI 设计决策（P3/P4/P5/P6/P7）；本阶段不做 | 待办 |

---

## 附：验证方法（可复现）

- 结构全量：对比 95 个角色 JSON 各层 key 集合（审计脚本，一次性）
- 数值抽样：7 代表角色逐字段对比源数据（`query.py` / 源 JSON 直读）
- 风险核查：已知陷阱清单逐项确认
- 回归：`python -m pytest tests/ -v`（84 用例）+ `pnpm build` + `pnpm test`（101 用例）
