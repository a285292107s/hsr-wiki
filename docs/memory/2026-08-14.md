# 2026-08-14

## 成就目录页档案索引风重构（taste-skill 应用）
- 触发：用户要求重构 achievement 列表页「高端、有品味、反 AI 味」→ 按 taste-skill 执行 Design Read（科幻档案/HUD 语言）+ 一次方向确认（选择档案索引风）。
- **改动闭环**（2 文件，未碰共享引擎）：`src/app/catalog/pages/achievement.ts`（卡片 HTML + `virtualImgRatio 0.26→0.22`、`virtualInfoH 140→128`、新增 subtitle ACHIEVEMENT INDEX）+ `src/styles/achievement.css` 全量重写。
- **反 AI 味硬约束已固化进 CSS 头部注释**（禁止回退）：禁 head 渐变 / 禁 glow / 禁 backdrop-filter 徽章 / 禁虚线分隔 / hover 禁位移（虚拟滚动抖动）。
- 工程要点：行高预算 `rowH = colW*ratio + infoH + 22`；desc 含 pre-line 空行时逻辑高可达 6 行（108px，clamp 5 + 领先/尾随换行），预算注释已按实测写明（5 行×18 + 空行余量）。
- 验证：build + 238 单测全绿；Playwright 探针双断点（1440/375）零溢出、AI 味清零（headBgImage/badgeShadow/badgeBackdrop 全 none、色条 2px）。

## 虚拟网格动画测量假象（探针返工 ≥2 轮的根因）
- 现象：溢出检测显示 36/36 卡均匀 +4px 溢出；hover 阴影捕获到全透明中间帧。
- 根因链：① 卡片入场动画 `nk-card-in`（from translateY(14px)）+ `backwards` 填充 → 动画 delay 期/滚动未 reveal 的卡片恒为 `opacity:0 + translateY(14px)`，`getBoundingClientRect().bottom` 多出 14px（-10 padding = +4 假象）；② `[].every(...)` 对空数组恒 true → waitForFunction 秒过（跳过所有未归位卡片后条件恒真）。
- 处置（已验证）：溢出统计必须过滤 `opacity==='1' && transform==='none'` 的卡片；等待条件必须带 `visible.length >= N` 前缀（防空数组）；hover 断言等阴影 alpha 特征（0.2/0.45）而非 `!== 'none'`（杜绝中间帧）。
- **坑位**：虚拟网格首屏稳定 reveal 数 < 视口卡片总数（IntersectionObserver threshold 0.5，缓冲行不触发）——等待门槛不得高于稳定值（桌面 1440×900 为 18 张、手机 375 为全量 10 张）。

## playwright 探针导入（沿用既有坑位记录）
- 一次性探针继续用 `import { chromium } from '@playwright/test'`（项目无 playwright 直接依赖，pnpm 严格结构解析不到），本次第一次运行即踩 ERR_MODULE_NOT_FOUND，已按 08-13 日志修正，未再展开排查。

## 待办/遗留
- 成就页无可点击跳转（cursor: default，无详情页）；若未来加详情路由，本页架构不变。
- e2e 无成就页断言（layout.spec 只覆盖首页/endgame/currency/character）；新增视觉基线前建议补 layout 用例或保持探针式验证。